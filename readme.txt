Clone the repository from https://github.com/krugert/WeatherWorkerService.git.
Open & run the Solution (.sln) file WeatherWorkerService.sln.
Change the web api values by changing the values in the WeatherWorkerService/appsettings.json file.
To run the tests build the application & run the tests in the Test Explorer of the WeatherWorkerService.Tests project.

